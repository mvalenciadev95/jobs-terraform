# Configuración para Windows
# Los scripts postinstall problemáticos se ignoran con --ignore-scripts en los comandos de instalación
# Esto evita errores con sintaxis de bash (||) que no funcionan en PowerShell

